name: CD

on:
  workflow_dispatch:

jobs:
  deploy-simulation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build custom app image
        run: docker build -t devops-demo-app ./docker/app

      - name: Simulate deployment stage
        run: echo "Deployment simulation completed successfully"
